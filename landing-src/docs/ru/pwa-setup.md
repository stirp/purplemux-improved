---
title: Настройка PWA
description: Добавьте purplemux-improved на главный экран в iOS Safari и Android Chrome для полноэкранного, app-подобного опыта.
eyebrow: Мобильные и удалённый доступ
permalink: /ru/docs/pwa-setup/index.html
---
{% from "docs/callouts.njk" import callout %}

Установка purplemux-improved как Progressive Web App превращает вкладку браузера в самостоятельную иконку на главном экране — с полноэкранным режимом и нормальными splash-экранами. На iOS это к тому же предусловие для Web Push.

## Что вы получите

- **Полноэкранный режим** — без хрома браузера, больше вертикального места для терминала и таймлайна.
- **Иконка приложения** — purplemux-improved запускается с главного экрана как любое нативное приложение.
- **Splash-экраны** — purplemux-improved поставляет splash-картинки для конкретных моделей iPhone, поэтому переход при запуске ощущается нативно.
- **Web Push** (только iOS) — push-уведомления работают только после установки PWA.

Manifest отдаётся на `/api/manifest` и регистрирует `display: standalone` со знаком purplemux-improved и цветом темы.

## Перед установкой

Страница должна быть доступна по **HTTPS**, чтобы PWA работал. С `localhost` это работает в Chrome (исключение для loopback), но iOS Safari не даст установить по обычному HTTP. Чистый путь — Tailscale Serve, см. [Доступ через Tailscale](/purplemux-improved/ru/docs/tailscale/).

{% call callout('warning', 'iOS требует Safari 16.4 или новее') %}
Более ранние релизы iOS могут установить PWA, но Web Push доставляться не будет. Если push для вас важен, сначала обновите iOS. Подробности по браузерам — в [Поддержке браузеров](/purplemux-improved/ru/docs/browser-support/).
{% endcall %}

## iOS Safari

1. Откройте URL purplemux-improved в **Safari** (другие iOS-браузеры не показывают «На экран Домой» для PWA).
2. Тапните иконку **Поделиться** на нижней панели.
3. Прокрутите шторку и выберите **На экран «Домой»**.
4. При желании поправьте имя и нажмите **Добавить** в правом верхнем углу.
5. Запустите purplemux-improved с новой иконки — он откроется на весь экран.

Первый запуск с иконки — это момент, когда iOS относится к нему как к настоящему PWA. Любой запрос на push должен инициироваться из этого standalone-окна, а не из обычной вкладки Safari.

## Android Chrome

Chrome автоматически замечает устанавливаемый manifest и показывает баннер. Если не видно:

1. Откройте URL purplemux-improved в **Chrome**.
2. Тапните меню **⋮** в правом верхнем углу.
3. Выберите **Установить приложение** (иногда называется **Добавить на главный экран**).
4. Подтвердите. Иконка появится на главном экране и в списке приложений.

Samsung Internet ведёт себя так же — приглашение к установке обычно появляется само.

## Проверка установки

Откройте purplemux-improved с иконки на главном экране. Адресной строки браузера быть не должно. Если UI браузера остался, manifest не применился — обычно потому, что страница загружена по обычному HTTP или через нестандартный прокси.

Также можно подтвердить через **Настройки → Уведомления**: как только PWA установлен и Web Push поддерживается, переключатель становится активным.

## Обновление PWA

Делать ничего не нужно. PWA загружает тот же `index.html`, что и ваш экземпляр purplemux-improved, поэтому обновление purplemux-improved обновляет установленное приложение при следующем запуске.

Чтобы убрать, долгое нажатие по иконке и выберите нативное удаление ОС.

## Что дальше

- **[Web Push уведомления](/purplemux-improved/ru/docs/web-push/)** — включите фоновые алерты, теперь когда PWA установлен.
- **[Доступ через Tailscale](/purplemux-improved/ru/docs/tailscale/)** — получите HTTPS-URL, который требует iOS.
- **[Поддержка браузеров](/purplemux-improved/ru/docs/browser-support/)** — полная матрица совместимости.
